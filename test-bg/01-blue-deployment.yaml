apiVersion: apps/v1
kind: Deployment
metadata:
  name: blue
spec:
  replicas: 2
  selector:
    matchLabels:
      color: blue
      dept: test
  template:
    metadata:
      labels:
        color: blue
        dept: test
    spec:
      containers:
        - name: nginx
          image: nginx:1.25.2